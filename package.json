{
  "name": "monolith-docs",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source document editing service built on OnlyOffice DocumentServer",
  "license": "AGPL-3.0-only",
  "workspaces": ["api"],
  "scripts": {
    "dev": "npm run dev --workspace=api",
    "build": "npm run build --workspace=api",
    "lint": "npm run lint --workspace=api",
    "typecheck": "npm run typecheck --workspace=api",
    "test": "npm run test --workspace=api"
  },
  "engines": {
    "node": ">=22"
  }
}
